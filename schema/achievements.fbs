namespace DS3C;

table Achievement{
  id: string;
  name: string; // markdown
  tasks: [Task];
}

table Task{
  id: string;
  text: string; // markdown
  note: string; // text, null for everything except Ring achievements
}

table AchievementsRoot{
  items: [Achievement];
}

root_type AchievementsRoot;
